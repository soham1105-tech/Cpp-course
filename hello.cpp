#include<iostream> //preprocessor directives start with'#' (inclusive here)
//some pre-defined/library functions are built in <iostream> like cin,cout,etc
using namespace std;
//The std namespace contains all standard C++ library components (like cout, cin, string, vector, etc.).
int main() { //OS starts executing the program from 'main' function.
    cout << "Hello World from Soham.";
    return 0; //exit code
}
string Soham() { //this is how function is defined, return type must be written.

}